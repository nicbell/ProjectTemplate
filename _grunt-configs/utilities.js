module.exports.tasks = {
    /**
     * Git Hooks
     * https://github.com/wecodemore/grunt-githooks
     * Run test task before any commit,
     * githooks will be generated on watch
     */
    githooks: {
        all: {
            /*'pre-commit': 'test',*/
        }
    }
};